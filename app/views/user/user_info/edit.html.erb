 <%= form_with model: @user, url:user_user_info_path(id: user.id) method: :patch  do |f| %>
  <label for="inputName">Name</label>
  <%= f.text_field :name, placeholder: user.name %></br>
  <%= f.submit "名前を変更" %>
<% end %>