<div class="search_genre_form">
  <label>ジャンル検索</label>
  <%= form_with url: user_genre_path, method: :get do |f| %>
  <%= f.select :id, options_from_collection_for_select(YouteGenre.all, :id, :name_was), include_blank: "--選択してください--" %>
  <%= f.submit '検索' %>
  <% end %>
</div>